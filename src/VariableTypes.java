public class VariableTypes {
  public static void main(String[] args) {
    /*
     * KIEU CO SO
     * 1. boolean => kieu ly luan => WraperClass: Bolean
     * 2. char => kieu ki tu => WraperClass: Char
     * 3. byte => so nguyen => WraperClass: Byte
     * 4. short => so nguyen => WraperClass: Short
     * 5. int => so nguyen => WraperClass: Integer
     * 6. long => so nguyen => WraperClass: Long
     * 7. float => so thuc => WraperClass: Float
     * 8. double => so thuc => WraperClass: Double
     * 9. 
     * 10.
     */
       
     //3. kieu so nguyen
     System.out.println("Max cua kieu int " + Integer.MAX_VALUE);
     System.out.println("Min cua kieu int " + Integer.MIN_VALUE);
  }
}
