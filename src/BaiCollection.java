import java.util.ArrayList;
import java.util.Collection;
import java.util.Collections;
import java.util.List;

public class BaiCollection {
  public static void main(String[] args) {
    // // 1. khai bao list
    // ArrayList<Integer> list = new ArrayList<>();
    // // 2. khai bao so luong phan tu ban dau
    // ArrayList<Integer> list2 = new ArrayList<>(5);
    // // 3. khoi tao voi cac phan tuu san co
    // ArrayList<Integer> list3 = new ArrayList<>(List.of(1, 2, 3, 4, 5));

    // // xuat list
    // System.out.println(list);
    // System.out.println(list2);
    // System.out.println(list3);

    // // add phan tu
    // ArrayList<Integer> lst = new ArrayList<>();
    // lst.add(1); // => add vao cuoi
    // lst.add(2); // => add vao cuoi
    // lst.add(3); // => add vao cuoi
    // lst.add(4); // => add vao cuoi
    // lst.add(0, 0); // => add vao in dex
    // System.out.println(lst);
    // // get size
    // System.out.println(lst.size());
    // // get
    // System.out.println(lst.get(0));
    // // remove
    // System.out.println(lst.remove(1)); // xoa index

    // // contains kiem tra phan tu trong list
    // ArrayList<Integer> lst = new ArrayList<>(List.of(1, 2, 3, 4, 5, 6));

    // boolean isExist = lst.contains(1);
    // System.out.println(isExist);

    // // sap sex sort
    // ArrayList<Integer> lst = new ArrayList<>(List.of(1, 2, 3, 4, 5, 6));
    // Collections.sort(lst);
    // System.out.println(lst);

    // // indexOf
    // ArrayList<Integer> lst = new ArrayList<>(List.of(1, 2, 3, 4, 5, 6));
    // System.out.println(lst.indexOf(4));

    // // duyet list
    // ArrayList<Integer> lst = new ArrayList<>(List.of(1, 2, 3, 4, 5, 6));
    // // c1. in gia tri
    // for (Integer item : lst) {
    // System.out.println(item);
    // }
    // // c2. duyet co ca index
    // for (int i = 0; i < lst.size(); i++) {
    // int item = lst.get(i);
    // System.out.println(item);
    // }
  }
}
